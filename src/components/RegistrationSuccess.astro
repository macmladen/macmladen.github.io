---
/** Shown in place of the form once a registration is in the database. The
 *  `data-form-success` attribute is the workshop page's handle on it: when the
 *  form is submitted with fetch, the script lifts this very block out of the
 *  rendered answer and puts it above the form.
 *
 *  Short on purpose since MM-70: everything else a registrant needs is in the
 *  confirmation email that is on its way to them.
 *
 *  The GitHub link carries `target` and `rel` in the source rather than getting
 *  them from `scripts/external-links.mjs`: /api/register/ renders this block at
 *  runtime, and a runtime response is never a file the postbuild step can
 *  rewrite (MM-76). */
import { workshop } from '../data/workshop';

interface Props {
  email: string;
  /** True when the last working seat had gone and the endpoint placed this
   *  registration as a watching one (MM-71). */
  watching?: boolean;
}

const { email, watching = false } = Astro.props;
---

<div class="notice notice--ok flow" data-form-success>
  <p><strong>You are on the list.</strong></p>
  {
    /* Approved by Mladen 2026-09-10: the line someone sees when the room filled up
       between loading the page and sending the form. */
    watching && (
      <p>
        The {workshop.capacity} working seats are taken, so you are registered to watch.
        Come along, follow the workshop and ask.
      </p>
    )
  }
  <p>
    A confirmation is on its way to {email}. The preparation guide and the demo project
    are <a href={workshop.repoUrl} target="_blank" rel="noopener">on GitHub</a>.
  </p>
</div>
